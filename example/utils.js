exports = {
    define: {
        path: "/example/utils",
    },
    left: function (str, length) {
        return $.left(str, length)
    },
    right: function (str, length) {
        return $.right(str, length)
    }
};